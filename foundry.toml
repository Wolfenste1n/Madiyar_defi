[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
script = "script"
solc = "0.8.20"
optimizer = true
optimizer_runs = 200
gas_reports = ["AMM", "LendingPool", "ERC20Token", "LPToken"]

[profile.default.fuzz]
runs = 256

[profile.default.invariant]
runs = 64
depth = 15
